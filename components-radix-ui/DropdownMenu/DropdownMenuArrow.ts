import * as DropdownMenuRadix from '@radix-ui/react-dropdown-menu';
import { styled } from '../../styles/stitches.config';

export const DropdownMenuArrow = styled(DropdownMenuRadix.Arrow, { fill: 'white' });
