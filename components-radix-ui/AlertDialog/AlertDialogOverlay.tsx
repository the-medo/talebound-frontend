import { styled } from '../../styles/stitches.config';
import * as AlertDialog from '@radix-ui/react-alert-dialog';
import { overlayStyles } from '../styles';

export const AlertDialogOverlay = styled(AlertDialog.Overlay, overlayStyles);
